# Copy to .env and fill in real values (`.env` is git-ignored).
# Docker Compose reads .env automatically for the Postgres service.
POSTGRES_DB=irkado
POSTGRES_USER=irkado
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
